# Network automation labs

These labs build from read-only Netmiko connections to backups, inventory-driven workflows, controlled configuration changes, and validation. Credentials must never be stored in source files or inventory data.

